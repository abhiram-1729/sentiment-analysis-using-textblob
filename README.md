# SentimentAI - Real-time Sentiment Analysis

A modern, responsive web application that performs real-time sentiment analysis on text input using TextBlob and FastAPI.

## 🚀 Features
- **Real-time Analysis**: Instant sentiment prediction (Positive/Negative).
- **Modern UI**: Sleek glassmorphism design with dynamic animations.
- **Visual Feedback**: Confidence bars and sentiment-themed color schemes.
- **RESTful Backend**: High-performance FastAPI backend.

## 🛠️ Tech Stack
- **Frontend**: HTML5, CSS3 (Vanilla), JavaScript (ES6+), Lucide Icons.
- **Backend**: Python, FastAPI, TextBlob.

## 💻 Local Setup

### 1. Prerequisite
Ensure you have Python 3.8+ installed.

### 2. Backend Setup
```bash
# Install dependencies
pip install -r requirements.txt

# Run the server
python main.py
```
The backend will be running at `http://localhost:8000`.

### 3. Frontend Setup
Simply open `index.html` in your favorite browser.

## ☁️ Deployment
The project is ready for deployment:
- **Backend**: Deploy to Render or Railway using the included `Procfile` and `requirements.txt`.
- **Frontend**: Deploy to Vercel or Netlify. Set the `VITE_API_URL` environment variable to your deployed backend URL.

---
*Powered by Scikit-Learn & FastAPI*
